{
  "name": "vela",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run infra:start && npm run dev:start",
    "stop": "npm run infra:stop && npm run dev:stop",
    "infra": "docker compose up -d",
    "infra:stop": "docker compose down",
    "dev": "sh start.sh",
    "dev:stop": "sh stop.sh"
  }
}
